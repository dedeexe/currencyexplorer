import UIKit

enum TimeType: String, CaseIterable {
    case day = "d"
    case minute = "min"
    case hour = "hour"
    case nothing = ""

    var isRightTupe: Bool {

    }
}

extension TimeType {
    init(value: String) {

    }
}
